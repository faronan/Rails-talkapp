<h1>グループ作成</h1>

<%= form_tag("/rooms/create_group") do %>
  <ul>
    <% if @users.any? %>
      <% @users.each do |user| %>
        <li><%= check_box "group_list", user.id %><%= user.name%></li>
      <% end %>
    <% end %>
  </ul>
  <input type="submit" value="作成">
<% end %>